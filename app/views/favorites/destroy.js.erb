$('#favorite_button').html(
  "<%= escape_javascript( render partial:'favorites/create_favorite', locals: {tweet: @tweet} )%>"
  );
$('#favorite_count').html("<%= escape_javascript( @tweet.favorites.count)%>");
